import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "mass-care.s3.eu-west-2.amazonaws.com"],
  },
};

export default nextConfig;
